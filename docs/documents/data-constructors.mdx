---
title: "Data constructors"
---

To pass a specific complex data type, use provided data constructors when inserting or querying your documents.

TopK provides constructors for the following complex types:

### u8_vector()

To pass a u8 vector, use the `u8_vector()` helper function:

<CodeGroup>

```python Python
from topk_sdk.data import u8_vector

u8_vector([0, 255, 1, 2, 3])
```


```typescript Javascript
import { u8Vector } from "topk-js/data";

u8Vector([0, 255, 1, 2, 3])
```

</CodeGroup>

### i8_vector()

To pass a i8 vector, use the `i8_vector()` helper function:

<CodeGroup>

```python Python
from topk_sdk.data import i8_vector

i8_vector([-128, 127, -1, 0, 1])
```


```typescript Javascript
import { i8Vector } from "topk-js/data";

i8Vector([-128, 127, -1, 0, 1])
```

</CodeGroup>

### binary_vector()

To pass a binary vector, use the `binary_vector()` helper function:

<CodeGroup>
```python Python
from topk_sdk.data import binary_vector

binary_vector([0, 1, 1, 0])
```


```typescript Javascript
import { binaryVector } from "topk-js/data";

binaryVector([0, 1, 1, 0])
```

</CodeGroup>

### f32_sparse_vector()

To pass a sparse vector with float32 values, use the `f32_sparse_vector()` helper function:

<CodeGroup>

```python Python
from topk_sdk.data import f32_sparse_vector

f32_sparse_vector({0: 0.12, 6: 0.67, 17: 0.82, 97: 0.53})
```


```typescript Javascript
import { f32SparseVector } from "topk-js/data";

f32SparseVector({0: 0.12, 6: 0.67, 17: 0.82, 97: 0.53})
```

</CodeGroup>

### u8_sparse_vector()

To pass a sparse vector with 8-bit unsigned integer values, use the `u8_sparse_vector()` helper function:

<CodeGroup>

```python Python
from topk_sdk.data import u8_sparse_vector

u8_sparse_vector({0: 12, 6: 67, 17: 82, 97: 53})
```


```typescript Javascript
import { u8SparseVector } from "topk-js/data";

u8SparseVector({0: 12, 6: 67, 17: 82, 97: 53})
```

</CodeGroup>

### f32_list()

To pass a list of 32-bit floating-point numbers, use the `f32_list()` helper function:


<CodeGroup>

```python Python
from topk_sdk.data import f32_list

f32_list([0.12, 0.67, 0.82, 0.53])
```


```typescript Javascript
import { f32List } from "topk-js/data";

f32List([0.12, 0.67, 0.82, 0.53])
```

</CodeGroup>

<Info>
  When passing an empty list or a list of numbers such as `my_list = [0.12, 0.67, 0.82]` or `my_list = []`, it is by default considered a list of float32 values.
</Info>

### f64_list()

To pass a list of 64-bit floating-point numbers, use the `f64_list()` helper function:

<CodeGroup>

```python Python
from topk_sdk.data import f64_list

f64_list([0.12, 0.67, 0.82, 0.53])
```


```typescript Javascript
import { f64List } from "topk-js/data";

f64List([0.12, 0.67, 0.82, 0.53])
```

</CodeGroup>

### i32_list()

To pass a list of 32-bit signed integers, use the `i32_list()` helper function:


<CodeGroup>

```python Python
from topk_sdk.data import i32_list

i32_list([0, 1, 2, 3])
```


```typescript Javascript
import { i32List } from "topk-js/data";

i32List([0, 1, 2, 3])
```

</CodeGroup>

### i64_list()

To pass a list of 64-bit signed integers, use the `i64_list()` helper function:


<CodeGroup>

```python Python
from topk_sdk.data import i64_list

i64_list([0, 1, 2, 3])
```



```typescript Javascript
import { i64List } from "topk-js/data";

i64List([0, 1, 2, 3])
```

</CodeGroup>

### u32_list()

To pass a list of 32-bit unsigned integers, use the `u32_list()` helper function:


<CodeGroup>

```python Python
from topk_sdk.data import u32_list

u32_list([0, 1, 2, 3])
```


```typescript Javascript
import { u32List } from "topk-js/data";

u32List([0, 1, 2, 3])
```

</CodeGroup>

### string_list()

To pass a list of strings, use the `string_list()` helper function:

<CodeGroup>

```python Python
from topk_sdk.data import string_list

string_list(["foo", "bar", "baz"])
```


```typescript Javascript
import { stringList } from "topk-js/data";

stringList(["foo", "bar", "baz"])
```

</CodeGroup>

<Tip>This is specifically useful when you need to pass an empty list of strings `string_list([])` as lists are assumed to be of type `float32` by default.</Tip>

### bytes()

To pass a byte object, use the `bytes()` helper function:

<CodeGroup>

```python Python
from topk_sdk.data import bytes

bytes([0, 1, 1, 0])
```


```typescript Javascript
import { bytes } from "topk-js/data";

bytes([0, 1, 1, 0]) # or pass a Buffer object
```

</CodeGroup>
